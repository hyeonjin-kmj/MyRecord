<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Piece1timer" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="barStyle">
        parent="Base.Widget.AppCompat.SeekBar.Discrete"
        app:tickColor = "@color/transparent"
    </style>

    <style name="Theme.Piece1timer" parent="Base.Theme.Piece1timer" />

    <style name="item">
        <item name="paddingStart">20dp</item>
        <item name="paddingEnd">20dp</item>
        <item name="android:layout_marginLeft">10dp</item>
        <item name="android:layout_marginRight">10dp</item>
        <item name="android:gravity">left</item>
        <item name="android:layout_marginTop">5dp</item>
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textSize">15dp</item>
        <item name="background">@drawable/daily_event_item</item>
        <item name="android:windowFullscreen">true</item>
    </style>



</resources>